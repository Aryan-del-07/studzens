{
  "name": "@studzens/database",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:format": "prisma format"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
