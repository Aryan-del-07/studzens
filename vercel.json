{
  "buildCommand": "npm install && npm run build --workspace=@studzens/frontend && cp -r frontend/dist dist",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

